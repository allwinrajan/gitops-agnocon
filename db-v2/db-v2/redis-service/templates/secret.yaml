apiVersion: v1
kind: Secret
metadata:
  name: redis-auth
  namespace: {{ include "redis.namespace" . }}
  labels:
    {{- include "redis.labels" . | nindent 4 }}
type: Opaque
stringData:
  redis-username: {{ .Values.auth.username | quote }}
  redis-password: {{ .Values.auth.password | quote }}
