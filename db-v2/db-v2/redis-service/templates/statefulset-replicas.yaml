---
# ==============================================================================
# StatefulSet: redis-replica-1  (pod: redis-replica-1-0)
#
# Role: Read replica. Replicates from primary via FQDN (headless DNS).
# Multus: net1 from macvlan-redis-replica1 NAD.
# Consul: connect-inject Envoy sidecar. consul-tag-patcher patches Multus IP.
# Resources: 1 CPU limit (read-only, lower write pressure).
# postStart: CONFIG SET notify-keyspace-events — replicas also publish events
#            (keyspace notifications work on replicas in read-only mode).
# PVC: reuses existing data-redis-replica-1-0 (10Gi on longhorn-redis).
# ==============================================================================
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: redis-replica-1
  namespace: {{ include "redis.namespace" . }}
  labels:
    {{- include "redis.labels" . | nindent 4 }}
    app.kubernetes.io/component: replica
spec:
  serviceName: {{ include "redis.headlessSvc" . }}
  replicas: 1
  selector:
    matchLabels:
      app: redis
      role: replica
      redis/pod: replica-1
  updateStrategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: redis
        role: replica
        redis/pod: replica-1
      annotations:
        {{- if .Values.multus.replica1.enabled }}
        k8s.v1.cni.cncf.io/networks: {{ .Values.multus.replica1.nadName | quote }}
        {{- end }}
        {{- if .Values.consul.enabled }}
        consul.hashicorp.com/connect-inject:        "true"
        consul.hashicorp.com/transparent-proxy:     "false"
        consul.hashicorp.com/connect-service:       {{ .Values.consul.serviceName | quote }}
        consul.hashicorp.com/transparent-proxy-exclude-inbound-ports:  {{ .Values.consul.excludeInboundPorts | quote }}
        consul.hashicorp.com/transparent-proxy-exclude-outbound-ports: {{ .Values.consul.excludeOutboundPorts | quote }}
        consul.hashicorp.com/service-meta-multus_ip:  "pending"
        consul.hashicorp.com/service-meta-multus_net: {{ .Values.multus.replica1.nadName | quote }}
        consul.hashicorp.com/service-meta-role:       "replica"
        {{- end }}
    spec:
      terminationGracePeriodSeconds: 15
      {{- if .Values.consul.enabled }}
      serviceAccountName: {{ .Values.consul.serviceAccountName }}
      {{- end }}

      initContainers:
        {{- include "redis.fixRouting" . | nindent 8 }}

        {{- if and .Values.consul.enabled .Values.multus.replica1.enabled }}
        {{- include "redis.consulTagPatcher" (dict
              "nadName" .Values.multus.replica1.nadName
              "port"    "6379"
              "role"    "replica"
              "ctx"     .) | nindent 8 }}
        {{- end }}

      containers:
        - name: redis
          image: {{ .Values.image.redis }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          # redis-server loads modules from redis.conf loadmodule directives.
          # replicaof injected as command-line arg (overrides redis.conf if present).
          command:
            - sh
            - -c
            - exec /entrypoint.sh --include /etc/redis/redis.conf --replicaof {{ include "redis.primaryFQDN" . }} 6379
          env:
            - name: REDIS_USERNAME
              valueFrom:
                secretKeyRef:
                  name: redis-auth
                  key: redis-username
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: redis-auth
                  key: redis-password
          ports:
            - name: redis
              containerPort: 6379
              protocol: TCP
          lifecycle:
            postStart:
              exec:
                command:
                  - sh
                  - -c
                  - |
                    PRIMARY="{{ include "redis.primaryFQDN" . }}"
                    # Wait for local Redis to be ready
                    for i in $(seq 1 30); do
                      redis-cli -a "${REDIS_PASSWORD}" --no-auth-warning PING 2>/dev/null | grep -q PONG && break
                      sleep 2
                    done
                    # Ensure replication is connected — retry if --replicaof arg didn't connect (DNS race at startup)
                    for attempt in $(seq 1 10); do
                      ROLE=$(redis-cli -a "${REDIS_PASSWORD}" --no-auth-warning INFO replication 2>/dev/null | grep "^role:" | tr -d '\r')
                      if [ "${ROLE}" = "role:slave" ] || [ "${ROLE}" = "role:replica" ]; then
                        echo "[postStart-r1] Replication connected (${ROLE})"
                        break
                      fi
                      echo "[postStart-r1] attempt ${attempt}: role=${ROLE} — issuing REPLICAOF ${PRIMARY} 6379"
                      redis-cli -a "${REDIS_PASSWORD}" --no-auth-warning REPLICAOF "${PRIMARY}" 6379 2>/dev/null || true
                      sleep 5
                    done
                    redis-cli -a "${REDIS_PASSWORD}" --no-auth-warning CONFIG SET notify-keyspace-events 'Exg$Klt' 2>/dev/null || true
                    echo "[postStart-r1] done"
                    exit 0
            preStop:
              exec:
                command:
                  - sh
                  - -c
                  - |
                    redis-cli -a "${REDIS_PASSWORD}" \
                      --no-auth-warning SHUTDOWN NOSAVE 2>/dev/null || true
                    exit 0
          {{- if .Values.probes.enabled }}
          livenessProbe:
            exec:
              command:
                - sh
                - -c
                - redis-cli -a "${REDIS_PASSWORD}" --no-auth-warning PING | grep -q PONG
            initialDelaySeconds: {{ .Values.probes.liveness.initialDelaySeconds }}
            periodSeconds:       {{ .Values.probes.liveness.periodSeconds }}
            timeoutSeconds:      {{ .Values.probes.liveness.timeoutSeconds }}
            failureThreshold:    {{ .Values.probes.liveness.failureThreshold }}
          readinessProbe:
            exec:
              command:
                - sh
                - -c
                - |
                  redis-cli -a "${REDIS_PASSWORD}" \
                    --no-auth-warning INFO replication | grep -qE "role:slave|role:replica"
            initialDelaySeconds: {{ .Values.probes.readiness.initialDelaySeconds }}
            periodSeconds:       {{ .Values.probes.readiness.periodSeconds }}
            timeoutSeconds:      {{ .Values.probes.readiness.timeoutSeconds }}
            failureThreshold:    {{ .Values.probes.readiness.failureThreshold }}
          {{- end }}
          resources:
            {{- toYaml .Values.resources.replica | nindent 12 }}
          volumeMounts:
            - name: data
              mountPath: /data
            - name: config
              mountPath: /etc/redis

      volumes:
        - name: config
          configMap:
            name: redis-config

  # PVC — matches existing data-redis-replica-1-0 (10Gi)
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        storageClassName: {{ .Values.storage.storageClass | quote }}
        resources:
          requests:
            storage: {{ .Values.storage.replica.size }}

---
# ==============================================================================
# StatefulSet: redis-replica-2  (pod: redis-replica-2-0)
# ==============================================================================
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: redis-replica-2
  namespace: {{ include "redis.namespace" . }}
  labels:
    {{- include "redis.labels" . | nindent 4 }}
    app.kubernetes.io/component: replica
spec:
  serviceName: {{ include "redis.headlessSvc" . }}
  replicas: 1
  selector:
    matchLabels:
      app: redis
      role: replica
      redis/pod: replica-2
  updateStrategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: redis
        role: replica
        redis/pod: replica-2
      annotations:
        {{- if .Values.multus.replica2.enabled }}
        k8s.v1.cni.cncf.io/networks: {{ .Values.multus.replica2.nadName | quote }}
        {{- end }}
        {{- if .Values.consul.enabled }}
        consul.hashicorp.com/connect-inject:        "true"
        consul.hashicorp.com/transparent-proxy:     "false"
        consul.hashicorp.com/connect-service:       {{ .Values.consul.serviceName | quote }}
        consul.hashicorp.com/transparent-proxy-exclude-inbound-ports:  {{ .Values.consul.excludeInboundPorts | quote }}
        consul.hashicorp.com/transparent-proxy-exclude-outbound-ports: {{ .Values.consul.excludeOutboundPorts | quote }}
        consul.hashicorp.com/service-meta-multus_ip:  "pending"
        consul.hashicorp.com/service-meta-multus_net: {{ .Values.multus.replica2.nadName | quote }}
        consul.hashicorp.com/service-meta-role:       "replica"
        {{- end }}
    spec:
      terminationGracePeriodSeconds: 15
      {{- if .Values.consul.enabled }}
      serviceAccountName: {{ .Values.consul.serviceAccountName }}
      {{- end }}

      initContainers:
        {{- include "redis.fixRouting" . | nindent 8 }}

        {{- if and .Values.consul.enabled .Values.multus.replica2.enabled }}
        {{- include "redis.consulTagPatcher" (dict
              "nadName" .Values.multus.replica2.nadName
              "port"    "6379"
              "role"    "replica"
              "ctx"     .) | nindent 8 }}
        {{- end }}

      containers:
        - name: redis
          image: {{ .Values.image.redis }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          # redis-server loads modules from redis.conf loadmodule directives.
          # replicaof injected as command-line arg (overrides redis.conf if present).
          command:
            - sh
            - -c
            - exec /entrypoint.sh --include /etc/redis/redis.conf --replicaof {{ include "redis.primaryFQDN" . }} 6379
          env:
            - name: REDIS_USERNAME
              valueFrom:
                secretKeyRef:
                  name: redis-auth
                  key: redis-username
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: redis-auth
                  key: redis-password
          ports:
            - name: redis
              containerPort: 6379
              protocol: TCP
          lifecycle:
            postStart:
              exec:
                command:
                  - sh
                  - -c
                  - |
                    PRIMARY="{{ include "redis.primaryFQDN" . }}"
                    for i in $(seq 1 30); do
                      redis-cli -a "${REDIS_PASSWORD}" --no-auth-warning PING 2>/dev/null | grep -q PONG && break
                      sleep 2
                    done
                    for attempt in $(seq 1 10); do
                      ROLE=$(redis-cli -a "${REDIS_PASSWORD}" --no-auth-warning INFO replication 2>/dev/null | grep "^role:" | tr -d '\r')
                      if [ "${ROLE}" = "role:slave" ] || [ "${ROLE}" = "role:replica" ]; then
                        echo "[postStart-r2] Replication connected (${ROLE})"
                        break
                      fi
                      echo "[postStart-r2] attempt ${attempt}: role=${ROLE} — issuing REPLICAOF ${PRIMARY} 6379"
                      redis-cli -a "${REDIS_PASSWORD}" --no-auth-warning REPLICAOF "${PRIMARY}" 6379 2>/dev/null || true
                      sleep 5
                    done
                    redis-cli -a "${REDIS_PASSWORD}" --no-auth-warning CONFIG SET notify-keyspace-events 'Exg$Klt' 2>/dev/null || true
                    echo "[postStart-r2] done"
                    exit 0
            preStop:
              exec:
                command:
                  - sh
                  - -c
                  - |
                    redis-cli -a "${REDIS_PASSWORD}" \
                      --no-auth-warning SHUTDOWN NOSAVE 2>/dev/null || true
                    exit 0
          {{- if .Values.probes.enabled }}
          livenessProbe:
            exec:
              command:
                - sh
                - -c
                - redis-cli -a "${REDIS_PASSWORD}" --no-auth-warning PING | grep -q PONG
            initialDelaySeconds: {{ .Values.probes.liveness.initialDelaySeconds }}
            periodSeconds:       {{ .Values.probes.liveness.periodSeconds }}
            timeoutSeconds:      {{ .Values.probes.liveness.timeoutSeconds }}
            failureThreshold:    {{ .Values.probes.liveness.failureThreshold }}
          readinessProbe:
            exec:
              command:
                - sh
                - -c
                - |
                  redis-cli -a "${REDIS_PASSWORD}" \
                    --no-auth-warning INFO replication | grep -qE "role:slave|role:replica"
            initialDelaySeconds: {{ .Values.probes.readiness.initialDelaySeconds }}
            periodSeconds:       {{ .Values.probes.readiness.periodSeconds }}
            timeoutSeconds:      {{ .Values.probes.readiness.timeoutSeconds }}
            failureThreshold:    {{ .Values.probes.readiness.failureThreshold }}
          {{- end }}
          resources:
            {{- toYaml .Values.resources.replica | nindent 12 }}
          volumeMounts:
            - name: data
              mountPath: /data
            - name: config
              mountPath: /etc/redis

      volumes:
        - name: config
          configMap:
            name: redis-config

  # PVC — matches existing data-redis-replica-2-0 (10Gi)
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        storageClassName: {{ .Values.storage.storageClass | quote }}
        resources:
          requests:
            storage: {{ .Values.storage.replica.size }}