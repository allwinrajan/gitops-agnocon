# redis-service — staging overlay
# Merged on top of values.yaml:  helm -f values.yaml -f values-staging.yaml

image:
  redis: "redis/redis-stack-server:7.2.0-v10"

consul:
  token: "693c91b6-88d9-87b2-3331-e6025c8f2cb8"
