# fs-core — production overlay
# Merged on top of values.yaml:  helm -f values.yaml -f values-production.yaml

image:
  tag: "fs-core-production-http-cache-v1.1.0"

consul:
  token: "PROD-CONSUL-TOKEN-REPLACE-ME"
