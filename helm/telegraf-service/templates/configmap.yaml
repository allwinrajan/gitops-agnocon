apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "telegraf-service.fullname" . }}-config
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "telegraf-service.labels" . | nindent 4 }}
data:
  telegraf.conf: |
{{ .Files.Get "configs/telegraf.conf" | indent 4 }}
